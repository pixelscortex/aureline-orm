//! Command-line entry point reserved for the CLI/configuration slice (#53).
//!
//! Generation and checking currently live in libraries so callers can exercise
//! them offline. The binary stays intentionally empty until configuration,
//! atomic migration writes, and exit-code behavior are implemented together.

fn main() {}
