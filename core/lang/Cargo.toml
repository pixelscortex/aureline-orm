[package]
name = "aureline-lang"
description = "Facade over Aureline AST, parser, checker, and migration crates."
edition.workspace = true
license.workspace = true
rust-version.workspace = true
version.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
aureline-ast = { path = "../ast" }
aureline-checker = { path = "../checker" }
aureline-migration = { path = "../migration" }
aureline-parser = { path = "../parser" }

[lints]
workspace = true
