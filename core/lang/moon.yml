id: aureline-lang
language: rust

tags:
  - rust
  - lang
  - facade

dependsOn:
  - aureline-ast
  - aureline-parser
  - aureline-checker
  - aureline-migration

tasks:
  build:
    command: "cargo build -p aureline-lang"
    deps:
      - "aureline-ast:build"
      - "aureline-parser:build"
      - "aureline-checker:build"
      - "aureline-migration:build"
    inputs: &rust_inputs
      - "src/**/*.rs"
      - "Cargo.toml"
      - "/Cargo.toml"
      - "/Cargo.lock"
  check:
    command: "cargo check -p aureline-lang"
    deps:
      - "aureline-ast:check"
      - "aureline-parser:check"
      - "aureline-checker:check"
      - "aureline-migration:check"
    inputs: *rust_inputs
  test:
    command: "cargo test -p aureline-lang"
    deps:
      - "aureline-ast:test"
      - "aureline-parser:test"
      - "aureline-checker:test"
      - "aureline-migration:test"
    inputs: *rust_inputs
  format:
    command: "cargo fmt --package aureline-lang --check"
    inputs:
      - "src/**/*.rs"
      - "Cargo.toml"
