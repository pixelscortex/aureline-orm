[package]
name = "aureline-ast"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Aureline syntax tree and shared language data structures."
license.workspace = true

[lib]
path = "src/lib.rs"

[features]
contract-serde = []

[dependencies]
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
