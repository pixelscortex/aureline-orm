[package]
name = "aureline-test"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Private contract-test harness for Aureline compiler stages."
license.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
aureline-parser = { path = "../../core/parser", features = ["unstable-test-normalization"] }
chumsky = "0.13.0"
serde = "1"

[lints]
workspace = true
