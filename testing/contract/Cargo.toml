[package]
name = "aureline-test"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Private Aureline contract-test harness."
license.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
aureline-parser = { path = "../../core/parser", features = ["unstable-test-normalization"] }
chumsky = "0.13.0"
serde = "1"

[dev-dependencies]
aureline-ast = { path = "../../core/ast" }

[lints]
workspace = true
